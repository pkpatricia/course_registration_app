<div class="col-md-6 col-md-offset-3 space">
<%= bootstrap_form_for [:admin, instructor] do |f| %>
  <%= f.text_field :name %>
  <div class="text-center space-bottom"><%= f.submit class: 'btn btn-primary' %></div>
<%- end %>
</div>